<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.project1">

    <application
        android:allowBackup="true"
        android:icon="@drawable/dxd_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/dxd_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Beranda"></activity>
        <activity android:name=".DaftarMenuAdd" />
        <activity android:name=".DaftarMenu" />
        <activity android:name=".DaftarFitur" />
        <activity android:name=".Welcome.Welcome1" />
        <activity android:name=".Welcome.Welcome" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>